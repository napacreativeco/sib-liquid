<ul class="hero-footer-links">
{% for link in linklists.footer.links %}
    <li>
        <a 
          style="color: color !important;" 
          href="{{ link.url }}" 
          title="{{ link.title }}">{{ link.title }}</a>
    </li>
{% endfor %}
</ul>
