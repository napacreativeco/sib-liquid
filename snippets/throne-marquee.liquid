{% style %}
  .marquee-holder {
    background-color: {{ settings.marquee_background_color | default: '#B6FFFE' }};
  }
  .marquee-holder .box {
    color: {{ settings.marquee_text_color | default: '#000000' }};
  }
{% endstyle %}

<div class="marquee-holder">
  <div class="wrapper">
    <div class="box">&nbsp;{{ settings.marquee_content }}</div>
    <div class="box">&nbsp;{{ settings.marquee_content }}</div>
    <div class="box">&nbsp;{{ settings.marquee_content }}</div>
    <div class="box">&nbsp;{{ settings.marquee_content }}</div>
    <div class="box">&nbsp;{{ settings.marquee_content }}</div>
    <div class="box">&nbsp;{{ settings.marquee_content }}</div>
    <div class="box">&nbsp;{{ settings.marquee_content }}</div>
    <div class="box">&nbsp;{{ settings.marquee_content }}</div>
    <div class="box">&nbsp;{{ settings.marquee_content }}</div>
    <div class="box">&nbsp;{{ settings.marquee_content }}</div>
    <div class="box">&nbsp;{{ settings.marquee_content }}</div>
    <div class="box">&nbsp;{{ settings.marquee_content }}</div>
  </div>
</div>