<div class="four-oh-four">
  <div class="four-oh-four-wrapper">
    <div class="words">
      <div>
        <h2>{{ '404' | escape }} / {{ 'ERROR' | escape }}</h2>
      </div>
    </div>
    <div class="images">
      <img src="{{ settings.fourohfour_image | image_url: width: 800 }}" alt="{{ '404' | escape }}" />
    </div>
  </div>
</div>