<div class="new-lookbook-page">

    {% comment %} 
    Things to Try:

    1. Metaobjects
    2. Assigning sections, then looping them
    {% endcomment %}
    [new lookbook page]

    {% for blog in shop.blogs %}
        {{ blog.title }}
    {% endfor %}
    <script>
        console.log('{{ blog | json }}');
    </script>
</div>