<div class="info-page">
    <div class="info-wrapper">
        <aside class="sidebar">
            {% for link in linklists.main-menu.links %}
                
            {% endfor %}
        </aside>
        {{ page.title }}
        {{ page.content }}
    </div>
</div>